[dataset]
folder = ./data         ; Root directory containing ESA mission data files
mission = 1             ; Mission number/identifier
period = 42_months       ; Time period of data
type = train            ; Dataset type (train, val, or test)
valid_split = 0.2       ; Split of the test dataset to use for validation


[windows]
stride = 15               ; Step size between consecutive samples
